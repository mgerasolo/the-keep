# Project Process Compliance Rules
# Generated by /augie:setup
# Customize these rules for your project's workflow standards

version: "1.0"
scope: process
enabled: true

# Will be populated by /augie:setup based on project type
rules: []

# Example rules (uncomment and customize as needed):
#
# - id: tdd-required
#   description: "Tests must be written before implementation (TDD)"
#   severity: warning
#   enabled: true
#   checks:
#     - test_files_exist: true
#     - test_before_implementation: true
#   message: "Implementation found without corresponding tests."
#
# - id: pr-review-required
#   description: "All PRs must have at least one review"
#   severity: critical
#   enabled: true
#   checks:
#     - pr_reviews: ">=1"
#   message: "PR has no reviews."
#
# - id: no-direct-main-push
#   description: "No direct pushes to main branch"
#   severity: critical
#   enabled: true
#   checks:
#     - branch_protection: true
#   message: "Direct push to main detected."
#
# - id: issue-linked
#   description: "PRs must be linked to an issue"
#   severity: warning
#   enabled: true
#   checks:
#     - issue_linked: true
#   message: "PR not linked to an issue."
#
# - id: container-deployment
#   description: "Deploy as container to Banner (dev) or Hulk (prod)"
#   severity: critical
#   enabled: true
#   checks:
#     - deployment_target: ["banner", "hulk"]
#   message: "Deployment target must be Banner or Hulk, not dev server."
#
# - id: conventional-commits
#   description: "Commit messages must follow conventional commits format"
#   severity: info
#   enabled: true
#   checks:
#     - commit_format: "^(feat|fix|docs|style|refactor|test|chore)(\\(.+\\))?:"
#   message: "Commit message does not follow conventional format."
