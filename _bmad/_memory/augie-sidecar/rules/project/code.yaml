# Project Code Compliance Rules
# Generated by /augie:setup
# Customize these rules for your project's coding standards

version: "1.0"
scope: code
enabled: true

# Will be populated by /augie:setup based on project type
rules: []

# Example rules (uncomment and customize as needed):
#
# - id: semantic-css-tokens
#   description: "Use semantic CSS tokens instead of hardcoded colors"
#   severity: warning
#   enabled: true
#   checks:
#     - pattern: "#[0-9a-fA-F]{3,6}"
#       files: ["*.css", "*.scss", "*.tsx"]
#       message: "Hardcoded color found. Use semantic token."
#
# - id: reusable-components
#   description: "Use reusable components instead of custom implementations"
#   severity: warning
#   enabled: true
#   checks:
#     - pattern: "<table[^>]*>"
#       files: ["*.tsx", "*.jsx"]
#       message: "Custom table found. Consider using DataTable component."
#
# - id: dark-mode-support
#   description: "Verify component supports dark mode"
#   severity: info
#   enabled: true
#   checks:
#     - pattern: "background-color:|color:"
#       files: ["*.css", "*.scss"]
#       message: "Color property found. Verify dark mode compatibility."
#
# - id: no-dev-server-deployment
#   description: "Deploy as container, not dev server"
#   severity: critical
#   enabled: true
#   checks:
#     - pattern: "npm run dev|yarn dev"
#       files: ["*.sh", "Dockerfile"]
#       message: "Dev server command found in deployment script."
